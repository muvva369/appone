import { GetpercentPipe } from './getpercent.pipe';

describe('GetpercentPipe', () => {
  it('create an instance', () => {
    const pipe = new GetpercentPipe();
    expect(pipe).toBeTruthy();
  });
});
